name: neom_commerce
description: Commerce Details for Cyberneom.
version: 1.2.0
homepage: https://github.com/emmanuel-montoya/neom_commerce
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  neom_commons:
    git:
      url: git@github.com:Cyberneom/neom_commons.git
  neom_bands:
    git:
      url: git@github.com:emmanuel-montoya/neom_bands.git
  neom_posts:
    git:
      url: git@github.com:Cyberneom/neom_posts.git
  neom_audio_player:
    git:
      url: https://github.com/Cyberneom/neom_audio_player.git

#  in_app_purchase: ^3.1.11
  in_app_purchase_storekit: ^0.3.6+7
#  percent_indicator: ^4.2.3

  #not working for products
#  woosignal: ^3.8.0
#  just_audio: 0.9.35

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.4.6
  json_serializable: ^6.7.1
  flutter_lints: ^4.0.0


flutter:
